---
import "../styles/tiny-wave.css";

import { ClientRouter } from "astro:transitions";

import { AstroSeo } from "@astrolib/seo";

import Analytics from "./partials/Analytics.astro";
import { Font } from "astro:assets";

export interface Props {
  title: string;
}

const { title } = Astro.props;
const longTitle = title + " - workingon.studio";
const currentURL = new URL(Astro.url.pathname, Astro.site).href;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <Font cssVariable="--font-satoshi" preload />
    <ClientRouter />
    <AstroSeo
      title={longTitle}
      description="Create waves with your voice."
      canonical={currentURL}
      openGraph={{
        url: currentURL,
        title: title,
        description: "Create waves with your voice.",
        images: [
          {
            url: Astro.site + "tiny-wave/opengraph/site-default.webp",
            height: 630,
            width: 1200,
            alt: "Tinywave - Create waves with your voice.",
          },
        ],
        site_name: "workingon.studio",
        type: "site",
      }}
      ,
      twitter={{
        cardType: "summary_large_image",
        handle: "@prmack",
      }}
      ,
      additionalMetaTags={[
        {
          name: "twitter:image",
          content: Astro.site + "tiny-wave/opengraph/site-default.webp",
        },
        {
          name: "twitter:url",
          content: "https://wo.studio/projects/tiny-wave",
        },
      ]}
    />
    <meta name="theme-color" content="#FAFAF9" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/tiny-wave/favicon.svg" />
    <script
      is:inline
      type="module"
      src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"
    ></script>

    <meta name="generator" content={Astro.generator} />
    <Analytics productName="TinyWave" />
    <title>{title}</title>
  </head>
  <body id="top" class="mx-[5%] h-full">
    <slot />
  </body>
</html>
