---
import "@styles/good-boy.css";

import Analytics from "@layouts/partials/Analytics.astro";
import { AstroSeo } from "@astrolib/seo";
import { ClientRouter } from "astro:transitions";
import { Font } from "astro:assets";

export interface Props {
  title: string;
}

const { title } = Astro.props;
const longTitle = title + " - workingon.studio";
const currentURL = new URL(Astro.url.pathname, Astro.site).href;
---

<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <AstroSeo
      title={longTitle}
      description="Get the praise you crave. The only Figma plugin you need to feel like you’re doing a good job."
      canonical={currentURL}
      openGraph={{
        url: currentURL,
        title: title,
        description:
          "Get the praise you crave. The only Figma plugin you need to feel like you’re doing a good job.",
        images: [
          {
            url: Astro.site + "good-boy/opengraph/site-default.webp",
            height: 630,
            width: 1200,
          },
          {
            url: Astro.site + "good-boy/opengraph/site-square.webp",
            height: 1080,
            width: 1080,
          },
          {
            url: Astro.site + "good-boy/opengraph/site-minimum.webp",
            height: 315,
            width: 600,
          },
        ],
        site_name: "goodboyplugin.com",
        type: "site",
      }}
      ,
      twitter={{
        cardType: "summary_large_image",
        handle: "@prmack",
      }}
      ,
      additionalMetaTags={[
        {
          name: "twitter:image",
          content: Astro.site + "good-boy/opengraph/site-default.webp",
        },
        {
          name: "twitter:url",
          content: "https://goodboyplugin.com",
        },
      ]}
    />
    <meta name="theme-color" content="#FFFFFF" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg" href="/good-boy/favicon.svg" />
    <link rel="icon" type="image/png" href="/good-boy/favicon.png" />
    <Font cssVariable="--font-oswald" preload />
    <Font cssVariable="--font-satoshi" preload />
    <script
      is:inline
      type="module"
      src="https://code.iconify.design/iconify-icon/2.1.0/iconify-icon.min.js"
    >
    </script>
    <meta name="generator" content={Astro.generator} />
    <Analytics productName="Good Boy" />
    <ClientRouter />
  </head>
  <body>
    <slot />
  </body>
</html>
